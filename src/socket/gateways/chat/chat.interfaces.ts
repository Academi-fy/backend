export interface ChatTargetMutation {
  targetId: string;
  chatId: string;
}

export interface ChatCourseMutation {
  chatId: string;
  courseId: string;
}

export interface ChatClubMutation {
  chatId: string;
  clubId: string;
}
