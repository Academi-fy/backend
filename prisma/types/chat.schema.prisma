model Chat {
  id        String   @id @default(uuid()) @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  avatar    String?
  clubs     Club[]
  courses   Course[]
  messages  Message[]
  name      String
  targets   UserChat[]
  type      String

  @@map("chats")
}
